name: Job Output

on:
  workflow_dispatch
  
jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - name: set-output
        id: set-output
        shell: bash
        run: echo "age=22" >> $GITHUB_OUTPUT

      - name: get-output
        run: echo ${{ steps.set-output.outputs.age }}
